namespace FloodGate.SDK.Events
{
    public enum EventTypes
    {
        FlushQueue = 0,
        ConfigRequest = 1,
        FlagEvaluation = 2,
        FlagNotFound = 3,
        SetUser = 4,
        Shutdown = 5
    }
}
